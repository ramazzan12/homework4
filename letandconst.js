

function main() {
    const PI=Math.PI;
    let r = readLine();
    
    let area=parseFloat(PI*r*r);
    let perimeter=parseFloat(2*PI*r);
    
    console.log(area);
    console.log(perimeter);
}